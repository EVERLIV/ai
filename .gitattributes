# Скрипты и серверный код должны иметь LF даже при разработке на Windows —
# иначе bash на Linux падает с "bad interpreter: ...^M".
*.sh text eol=lf
*.mjs text eol=lf
*.service text eol=lf
*.sql text eol=lf
